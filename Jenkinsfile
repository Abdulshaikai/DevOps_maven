pipeline {
  agent any
  stages {
    stage ('build stage') {
      steps {
          echo "hello! world"
           echo "PATH = ${PATH}"
           echo "M2_HOME = ${M2_HOME}"

         }
    }
  }
}
